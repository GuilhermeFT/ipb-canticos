<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".View.BeginActivity" android:background="@drawable/back_fade" android:orientation="vertical"
        android:gravity="center">

    <ImageView
            android:layout_width="300dp"
            android:layout_height="300dp" app:srcCompat="@drawable/ic_icon_sarca"
            tools:layout_editor_absoluteY="275dp" tools:layout_editor_absoluteX="181dp" android:id="@+id/iv_logo"
            android:scaleType="fitCenter" tools:ignore="VectorDrawableCompat"/>

    <TextView
            android:id="@+id/tv_title"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:fontFamily="sans-serif-light"
            android:text="Cânticos Espirituais"
            android:textAlignment="center"
            android:textColor="#5C5C5C"
            android:textSize="30sp" />

    <TextView
            android:id="@+id/tv_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-light"
            android:text="Igreja Presbiteriana de Barra do Garças"
            android:textAlignment="center"
            android:textColor="#5C5C5C"
            android:textSize="24sp"
            android:textStyle="bold" />
</LinearLayout>